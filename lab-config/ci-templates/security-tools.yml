image: alpine:3.19

stages:
  - scan

scan:
  stage: scan
  script:
    - echo "Running security scan"
    - echo "Using token: ${SCANNER_TOKEN:0:6}****"
    - echo "Webhook: $REPORT_WEBHOOK"
  tags:
    - docker
