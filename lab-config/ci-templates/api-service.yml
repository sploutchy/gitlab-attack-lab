image: python:3.11

stages:
  - test
  - build

tests:
  stage: test
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - pytest -q
    - echo "Testing with API_KEY: ${API_KEY:0:6}****"
  tags:
    - docker

package:
  stage: build
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - python -m pip install build
    - python -m build
  artifacts:
    paths:
      - dist/
  tags:
    - docker
